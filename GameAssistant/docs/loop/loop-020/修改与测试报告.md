# 第 20 轮循环 - 修改与测试报告

**循环日期**: 2026-06-23 15:19
**轮次**: loop-020
**负责人**: Agent（开发 agent）

---

## 一、本次修改内容

### 1.1 修改文件
| 文件路径 | 修改内容 |
|---|---|
| `web/frontend/e2e/global-setup.ts` | 修复跨浏览器认证问题，手动保存 localStorage 中的 JWT token |
| `web/frontend/e2e/playwright.config.ts` | Mobile Chrome 使用独立的认证文件 `mobile-chrome.json` |

### 1.2 技术要点说明
> **问题根因**: Playwright 的 `context.storageState()` API 默认只保存 cookies，不保存 localStorage。但前端使用 localStorage 存储 JWT token，导致恢复认证状态后 token 丢失，引发 403 错误。
>
> **解决方案**: 在 `global-setup.ts` 中登录后，手动通过 `page.evaluate()` 获取 localStorage 数据，然后手动构建 storageState 对象，包含 cookies 和 origins 下的 localStorage 数据。
>
> **额外修复**: Mobile Chrome 原本与 chromium 共用认证文件，现改为独立文件 `mobile-chrome.json`。

### 1.3 关键代码变更

**global-setup.ts 核心变更**:
```typescript
// 新增：获取 localStorage 数据
async function getLocalStorageData(page: any): Promise<Record<string, string>> {
  return await page.evaluate(() => {
    const data: Record<string, string> = {}
    for (let i = 0; i < localStorage.length; i++) {
      const key = localStorage.key(i)
      if (key) {
        data[key] = localStorage.getItem(key) || ''
      }
    }
    return data
  })
}

// 手动构建 storageState（包含 localStorage）
const storageState = {
  cookies: cookies.map(cookie => ({...})),
  origins: [
    {
      origin: new URL(BASE_URL).origin,
      localStorage: Object.entries(localStorageData).map(([name, value]) => ({
        name,
        value,
      })),
    },
  ],
}
```

---

## 二、功能测试

### 2.1 测试环境
- 后端：Python 3.10+ / FastAPI (Docker: gameassistant_backend)
- 前端：Node.js / Vue 3.4+ / Vite (Docker: gameassistant_frontend)
- 数据库：MySQL 8.0 (Docker: gameassistant_mysql)
- 测试框架：Playwright 1.42.0

### 2.2 测试结果

| 浏览器 | 测试项数 | 通过 | 跳过 | 失败 | 结果 |
|---|---|---|---|---|---|
| Chromium | 33 | 30 | 3 | 0 | ✅ 通过 |
| Firefox | 33 | 30 | 3 | 0 | ✅ 通过 |
| WebKit | 33 | 30 | 3 | 0 | ✅ 通过 |
| Mobile Chrome | 33 | 30 | 3 | 0 | ✅ 通过 |

**跳过的测试**（login.spec.ts 中的 3 个测试需要无认证状态，不适用当前场景）：
- 登录成功 → 跳转到图片管理页
- 登录失败 → 显示错误提示
- 未登录访问受保护路由 → 重定向到登录页

### 2.3 认证文件验证
所有浏览器的认证文件（`.auth/*.json`）现在都包含 `localStorage` 数据：
```json
{
  "cookies": [],
  "origins": [
    {
      "origin": "http://localhost:3000",
      "localStorage": [
        {
          "name": "gameassistant_token",
          "value": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
        }
      ]
    }
  ]
}
```

### 2.4 未解决问题
> 无。本次循环问题已全部解决。

---

## 三、产出物清单

- [x] `web/frontend/e2e/global-setup.ts`（修改）
- [x] `web/frontend/e2e/playwright.config.ts`（修改）
- [x] `docs/loop/loop-020/修改与测试报告.md`（本文件）

---

## 四、备注

### 4.1 问题回顾
- **上一次循环问题**: Firefox/WebKit 认证失败（403错误）
- **根本原因**: Playwright storageState API 默认不保存 localStorage
- **修复方案**: 手动保存和恢复 localStorage 中的 JWT token

### 4.2 测试通过率
- **Chromium**: 30/30 通过
- **Firefox**: 30/30 通过
- **WebKit**: 30/30 通过
- **Mobile Chrome**: 30/30 通过
- **总计**: 120/120 通过（0 失败）
