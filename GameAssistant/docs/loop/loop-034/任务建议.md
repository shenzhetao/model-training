# 第 34 轮循环 - 任务建议

**评估日期**: 2026-06-23 22:00
**轮次**: loop-034
**评估人**: Agent（开发 agent）

---

## 一、本次循环评估

### 1.1 完成度评估

| 功能 | 优先级 | 实现状态 |
|---|---|---|
| 标注管理 Tab 切换测试修复 | P0 | ✅ 已完成并验证通过 |
| Mobile Chrome 测试隔离优化 | P0 | ✅ 已完成 |
| WebKit 专用测试添加 | P0 | ✅ 已完成并验证通过 |
| loop-034 文档创建 | P0 | ✅ 已完成 |

### 1.2 测试结果

| 浏览器 | 通过率 | 测试数 |
|---|---|---|
| Chromium | 100% (16/16) | annotations.spec.ts |
| Firefox | 100% (16/16) | annotations.spec.ts |
| WebKit | 100% (11/11) | webkit-compat.spec.ts |

### 1.3 质量问题

1. **API 错误日志**：测试中出现 500 错误和 CORS 预检失败，但测试仍通过，说明测试用例对后端 API 依赖较轻（只验证 UI 元素可见性）
2. **Mobile Chrome 未实际测试**：只完成了配置优化，实际运行测试需要后续验证

---

## 二、下一次循环（loop-035）开发建议

### 2.1 优先级 P0（必须完成）

- [ ] **提升整体测试通过率**
  - 当前 annotations.spec.ts 通过率已提升到 100%
  - 建议：运行完整测试套件，确认整体通过率提升

- [ ] **解决其他 spec 文件的失败测试**
  - 运行 `npx playwright test` 查看完整测试结果
  - 分析失败测试的具体原因
  - 针对性地修复或调整

### 2.2 优先级 P1（如有余力）

- [ ] **完善测试数据 Fixture**
  - 使用新创建的 test-data fixture 重构现有测试
  - 确保测试数据命名唯一性
  - 添加更完整的清理逻辑

- [ ] **添加 API Mock 层**
  - 使用 MSW 模拟后端 API
  - 使测试不依赖后端服务
  - 提高测试执行速度

- [ ] **验证 Mobile Chrome 测试**
  - 运行 `npx playwright test --project="Mobile Chrome"`
  - 验证配置优化的效果

### 2.3 本次遗留问题（需在下轮继续）

1. Mobile Chrome 测试实际运行效果待验证
2. 整体测试套件通过率需进一步提升
3. API 错误日志（500 错误）需关注

---

## 三、风险提示

1. **后端依赖风险**：虽然 UI 测试通过，但 API 错误日志表明后端服务可能存在问题
2. **Mobile Chrome 兼容性**：配置优化后需实际运行验证

---

## 四、测试运行建议

### 4.1 快速验证命令
```bash
cd web/frontend/e2e

# 验证 Tab 切换测试（重点）
npx playwright test tests/annotations.spec.ts --project=chromium
npx playwright test tests/annotations.spec.ts --project=firefox

# 验证 WebKit 兼容测试
npx playwright test tests/webkit-compat.spec.ts --project=webkit

# 验证 Mobile Chrome 配置
npx playwright test --project="Mobile Chrome"

# 运行完整测试套件
npx playwright test
```

### 4.2 完整测试命令
```bash
cd web/frontend/e2e

# 安装依赖（如果需要）
npm install

# 生成认证状态
npx playwright test --project=chromium-fresh

# 运行所有测试
npx playwright test
```
