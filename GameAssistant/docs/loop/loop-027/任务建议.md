# 第 27 轮循环 - 任务建议

**评估日期**: 2026-06-23 16:55
**轮次**: loop-027
**评估人**: Agent（评估 agent）

---

## 一、本次循环评估

### 1.1 完成度评估

| 功能 | 优先级 | 实现状态 |
|---|---|---|
| Vite 分包策略优化 | P0 | ✅ 已完成（最大 chunk 从 669KB 降至 175KB） |
| Playwright 测试配置 | P0 | ✅ 已完成（测试框架就绪） |
| 页面加载验证 | P0 | ✅ 已完成（登录页加载正常，无控制台错误） |
| 运行时验证（完整登录流程） | P0 | ⚠️ 未完成（后端 API 未启动） |

### 1.2 质量问题

1. **运行时验证不完整**：虽然页面加载测试通过，但完整的登录流程测试因后端未启动而跳过
2. **ECharts chunk 仍较大**：520KB 的 echarts chunk 可考虑进一步优化
3. **未进行生产构建验证**：优化后的构建需要在实际环境中验证

---

## 二、下一次循环（loop-028）开发建议

### 2.1 优先级 P0（必须完成）

- [ ] **完整运行时验证**
  - 启动后端服务（FastAPI）
  - 运行 Playwright 测试验证完整登录流程
  - 验证 TrainingManager 页面的 Loss/mAP/Precision-Recall 图表正常渲染
  - 检查控制台无错误

- [ ] **生产构建验证**
  - 使用 `vite preview` 预览生产构建
  - 验证首屏加载性能改善

### 2.2 优先级 P1（如有余力）

- [ ] **ECharts 延迟加载优化**
  - 将 ECharts 图表组件改为真正的动态导入
  - 只在用户实际访问 TrainingManager 等图表页面时才加载 ECharts

- [ ] **HTTP 缓存头配置**
  - 后端添加 Cache-Control 响应头
  - 静态资源使用长期缓存策略

- [ ] **Web Vitals 监控**
  - 添加 LCP、FID、CLS 性能指标监控
  - 在开发环境输出性能日志

### 2.3 本次遗留问题（需在下轮继续）

> 本次循环完成了首屏性能优化（chunk 拆分）和 Playwright 测试框架验证。完整的运行时验证需要后端服务支持，建议在下轮继续。

---

## 三、风险提示

1. **后端依赖**：完整测试需要后端服务运行，需确保 FastAPI 服务正常
2. **测试环境隔离**：Playwright 测试需要独立的后端数据库，避免污染开发数据
3. **图表渲染验证**：TrainingManager 的 ECharts 图表需要真实数据才能完全验证
