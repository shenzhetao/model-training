# 第 28 轮循环 - 任务建议

**评估日期**: 2026-06-23 17:10
**轮次**: loop-028
**评估人**: Agent（评估 agent）

---

## 一、本次循环评估

### 1.1 完成度评估

| 功能 | 优先级 | 实现状态 |
|---|---|---|
| 后端服务验证 | P0 | ✅ 已完成（FastAPI + MySQL 正常运行） |
| 登录 API 验证 | P0 | ✅ 已完成（JWT 认证正常） |
| Playwright 登录测试 | P0 | ✅ 已完成（重写测试支持已登录状态） |
| 关键页面访问测试 | P0 | ✅ 已完成（/images, /training, /inference） |
| 生产构建验证 | P0 | ✅ 已完成（构建成功 + preview 正常） |
| 完整运行时验证 | P0 | ✅ 已完成（端到端流程可工作） |

### 1.2 质量问题

1. **Playwright auth state 管理**：auth state 被全局复用，导致登录页测试无法验证登录表单显示。建议后续在测试中显式清除 auth state。
2. **CORS 错误**：开发服务器中 API 代理存在 CORS 问题，但 Docker 生产环境中无此问题。
3. **ECharts chunk 仍较大**：520KB 的 ECharts chunk 可考虑延迟加载。

---

## 二、下一次循环（loop-029）开发建议

### 2.1 优先级 P0（必须完成）

- [ ] **Playwright 测试套件完善**
  - 修复 auth state 管理，确保登录表单测试能正常执行
  - 在无 auth state 的新 context 中测试登录流程
  - 添加登录失败的测试用例

- [ ] **训练管理页面图表验证**
  - 验证 TrainingManager 页面的 Loss/mAP/Precision-Recall 图表正常渲染
  - 确认 ECharts 图表在有数据时的显示效果

- [ ] **E2E 测试环境优化**
  - 配置 Playwright 使用生产 API 端点
  - 解决开发服务器的 CORS 代理问题

### 2.2 优先级 P1（如有余力）

- [ ] **ECharts 延迟加载优化**
  - 将 ECharts 改为真正的动态导入
  - 只在用户访问图表页面时才加载

- [ ] **HTTP 缓存头配置**
  - 后端添加 Cache-Control 响应头
  - 静态资源使用长期缓存策略

- [ ] **Web Vitals 监控**
  - 添加 LCP、FID、CLS 性能指标监控
  - 在开发环境输出性能日志

### 2.3 本次遗留问题（需在下轮继续）

> 本次循环完成了完整运行时验证的核心任务。遗留问题主要是测试层面的优化，不影响功能正常使用。

---

## 三、风险提示

1. **Playwright 测试不稳定**：auth state 管理问题可能导致测试在某些情况下失败
2. **ECharts 性能**：520KB 的 ECharts 包可能影响首屏加载性能
3. **API 代理配置**：开发服务器的 CORS 配置需要检查 vite.config.ts 中的 proxy 设置
